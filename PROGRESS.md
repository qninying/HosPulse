# PROGRESS

## Setup

- [x] Create project folder, README, CLAUDE.md, prospect list
  - Date: 2026-09-22
  - What changed: initial repo scaffold; `docs/research/rural_hospital_mgmt_companies.csv` (10 prospects, ranked)
  - Verification: files present, private GitHub repo created

- [x] Blueprint: architecture, tech stack, MVP plan, mockup, one-pager
  - Date: 2026-09-22
  - What changed: `project-blueprint/` (architecture.md, tech-stack.md, mvp-plan.md, mockup.html, one-pager.html/.pdf, colaberry-idea-prompt.md)
  - Verification: one-pager rendered to a real 1-page PDF with headless Chrome; mockup screenshot checked

- [x] Plan and Command Center (CoreOps structure)
  - Date: 2026-09-22
  - What changed: `.hospulse/plan.json` (19 requirements, 12 stories, 5 releases, 2 agents); `scripts/build_plan.py` generates docs, progress and manifest; `docs/` REQUIREMENTS, STORIES, TRACEABILITY, DATA_CONTRACT, stories/STORY-000..012; `command-center/` 9 tabs
  - Verification: `python3 -m unittest discover scripts` 6 tests OK; second build run prints "no changes"; 17 pages x 2 modes rendered in the browser with no console errors
  - Notes: STORY-000 has 3 of 5 criteria passed; the 2 that require `.hospulse/` to be committed stay false until the first commit. Preview launcher cannot read ~/Desktop (macOS permission), so verification used a temporary `python3 -m http.server`.

- [x] Make repo public and connect to the Colaberry platform
  - Date: 2026-09-22
  - What changed: squashed git history to a single commit (removed a prospect's name that appeared in the original first commit), repo set to public, `.colaberry/connect.txt` committed, push webhook to enterprise.colaberry.ai registered, `.colaberry/progress.json` (platform-generated, 14 stories) added
  - Verification: fetched and text-searched the actual GitHub history post-push, zero matches for the prospect's name; `gh api .../hooks` confirms the webhook is registered and active

- [x] Grounding guardrail: reject any AI output with an unsupported dollar/percent/days figure
  - Date: 2026-09-22
  - What changed: `pipeline/grounding_guardrail.py` (`validate_ai_output_is_grounded`, shared by the briefing agent and the cost report co-pilot), `pipeline/test_grounding_guardrail.py`; added a concrete acceptance criterion to STORY-011 naming this module, replacing prose-only coverage; regenerated `docs/stories/STORY-011.md`
  - Verification: `.venv/bin/python -m pytest pipeline/ -v` 16/16 passed; `python3 scripts/build_plan.py` run twice, second run "no changes"; `python3 -m unittest discover scripts` 6/6 passed

- [x] Repoint Command Center at the Colaberry platform's own plan (STORY-000, platform-side)
  - Date: 2026-09-22
  - What changed: root `index.html` redirect to `command-center/index.html` (GitHub Pages entry point requirement); `command-center/assets/command-center.js` DATA_DIR switched from `.hospulse` to `.colaberry`; added `CommandCenter.ownersFromPlan()` fallback since this plan has no `derived.owners`; `command-center/agents.html` updated to use it; `plan.schedule` null-guarded in `init()`; `.colaberry/plan.json` and `manifest.json` added (plan.json's `schedule`/release/story dates backfilled from the portal's STORY-000 text, since the generated seed had `schedule: null`); `.colaberry/progress.json` STORY-000 criteria ticked (5/5) with evidence in `notes`; `.colaberry/enrichment/STORY-000.json` written; `docs/stories/STORY-000.md` replaced with the platform's current version; `CLAUDE.md` documents the `.colaberry` (Command Center, platform-owned) vs `.hospulse` (private build plan) split; GitHub Pages turned on (`https://qninying.github.io/HosPulse/`)
  - Verification: `node --check` clean on all JS (shared file + every inline script block); every `plan.X`/`progress.X` field referenced in the codebase traced against the real committed JSON; `ownersFromPlan`/`verificationForRequirement` run in Node against the real data, output matched the portal's hand-specified owner breakdown exactly; all 9 tabs + root redirect + 3 data files returned HTTP 200 from a local server
  - Notes: no live browser available this session to visually confirm rendering; verification was static (syntax, field trace, logic replay), not an observed render. `.hospulse/plan.json` and its generated `docs/` remain as a separate, still-valid build plan; the Command Center no longer reads them.

- [x] STORY-001: import CMS HCRIS data into real Supabase Postgres
  - Date: 2026-09-22
  - What changed: Supabase project created (`hospulse`, us-east-2), automatic-RLS-on-new-tables event trigger enabled; `pipeline/schema.sql` (`hospitals`, `cost_report_years`, generated `operating_margin_pct`/`days_cash_on_hand`/`days_in_ar` columns, public-read RLS policies); `pipeline/hcris_import.py` (downloads the real CMS HCRIS zip, filters to OK/TX rural and Critical Access Hospitals, computes the 3 metrics, idempotent upsert); `pipeline/test_hcris_import.py` (12 tests); `requirements.txt` (psycopg2-binary, duckdb, python-dotenv); `.colaberry/enrichment/STORY-001.json`
  - Verification: real run against live Supabase -- 256 OK/TX rural/CAH hospitals, 260 cost-report-years for FY2024; re-run from a clean state produced an identical content hash (genuine idempotency, not just "no crash"); `pytest pipeline/test_hcris_import.py -v` 12/12 passed, including the invalid-format failure path; every worksheet/line/column code confirmed against a real downloaded report (RPT_REC_NUM 795252) before being trusted, not taken from documentation alone
  - Notes: two real bugs found and fixed while verifying, not left in: (1) DuckDB lazy-evaluation bug -- reusing one registered view name ("kv") for two sequential pivots caused the first to silently read the second file's data, returning 0 results with no error; fixed by materializing into real temp tables. (2) Fiscal-year mislabeling -- CMS's HOSP10FY2024.ZIP bundles reports by when CMS processed them, not each report's own period end, so a report ending 06/30/2025 was being stored as fiscal_year=2024; fixed by deriving fiscal_year from the report's own fy_end_dt, plus a deterministic dedup tie-break for genuine same-year duplicates (highest RPT_REC_NUM wins). `.colaberry/progress.json` STORY-001 ticked 3/3 (genuinely earned). `.hospulse/progress.json` STORY-001 ticked honestly at 1/4 against its own stricter criteria -- only FY2024 imported (not 3 years), no explicit "reason" column for missing values, worksheet/line provenance lives in code not per-row in the database. Both are real, neither rounded up.

- [x] Expand HCRIS import to FY2025 and FY2023 (latest 3 years complete)
  - Date: 2026-09-22
  - What changed: `pipeline/hcris_import.py` -- added a WHERE guard to the `cost_report_years` upsert (`EXCLUDED.source_rpt_rec_num >= cost_report_years.source_rpt_rec_num`) so an older file re-run after a newer one can never regress a row back to a worse report; ran the importer for FY2025 then FY2023
  - Verification: live-tested the regression guard directly against Supabase (a same-key upsert with a lower record number was blocked, a higher one applied, then reverted to the real value); full pipeline re-run three times, content hash identical on runs 2 and 3 (genuine idempotency); final state: 274 OK/TX rural/CAH hospitals, 652 cost-report-years across FY2023/2024/2025, zero duplicate keys, zero mislabeled fiscal years; `pytest pipeline/` 28/28 passed
  - Notes: `.hospulse/progress.json` STORY-001's "latest three years" criterion now genuinely true (2/4 total there); not every hospital has a row in all 3 years, which is a real reporting gap (closures, fiscal-year-end changes), not a bug -- forcing one would mean inventing data

## Next

- [x] Close STORY-001's two remaining gaps: per-metric provenance and a real "missing" reason
  - Date: 2026-09-22
  - What changed: `pipeline/schema.sql` -- `metric_provenance jsonb` column on `cost_report_years` (migration via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`, safe against the existing production table); `pipeline/hcris_import.py` -- pivot now tracks whether each worksheet line was present at all (not just its parsed value), builds a per-row provenance dict (`{wksht_cd, line_num, clmn_num, status}` per metric, status = ok / not_reported / unparseable), and a new `_dedupe_by_provider()` fixes a real crash found while backfilling; `pipeline/test_hcris_import.py` (2 more tests, 30 total)
  - Verification: schema migration applied live; backfilled all 652 existing rows (0 left with empty provenance); confirmed 18 rows genuinely have `cash_on_hand: not_reported` (a real distinction, not lost as an ambiguous NULL); full 3-fiscal-year re-run produced an identical content hash including provenance (idempotent); `pytest pipeline/` 30/30 passed; `.hospulse/progress.json` STORY-001 now 4/4, genuinely
  - Notes: real bug found while backfilling, not left in -- `hospitals` (keyed on `provider_ccn` alone) crashed on FY2025 with Postgres's "ON CONFLICT DO UPDATE command cannot affect row a second time," because a single run's batch can legitimately contain one hospital across two real fiscal years (the same split-year pattern found earlier in STORY-001). Fixed with a second, hospitals-scoped dedupe preferring the most recent fiscal year.

- [x] STORY-002: tick the genuinely-true criteria, flag the "monthly metrics" mismatch
  - Date: 2026-09-22
  - What changed: `.colaberry/progress.json` STORY-002 criteria 2 ("traced back to file and row") and 3 ("audit trail linking metrics to source data") marked true -- both were already genuinely built while hardening STORY-001 (`operating_margin_pct`/`days_cash_on_hand`/`days_in_ar` as `GENERATED ALWAYS AS STORED` columns; `metric_provenance` jsonb with per-metric worksheet/line/column/status). `.colaberry/enrichment/STORY-002.json` written with evidence-cited fact proposals, decisions and the limitation below.
  - Verification: field-level trace of `metric_provenance` against all 652 live Supabase rows (0 left empty, 18 confirmed genuinely `not_reported`); `pytest pipeline/` 30/30 passed
  - Notes: criterion 1 ("produce a standard set of **monthly** metrics") left unticked on purpose, not rounded up. CMS HCRIS cost reports are annual-only -- there is no month anywhere in the source data to slice. REQ-002 (the requirement STORY-002 actually fulfills) correctly says "per hospital per year"; "monthly metrics" is REQ-011/STORY-011's language (operator-uploaded monthly exports, release r2) and appears to have been copied into this story's acceptance criteria by mistake. Raised with the user and confirmed before leaving the line unticked rather than rewording or faking it.

- [x] STORY-010: reject files with patient names, birth dates, or record numbers before storage or logging
  - Date: 2026-09-23
  - What changed: `pipeline/phi_guardrail.py` -- `scan_file_for_phi()` detects REQ-009's three categories (patient names, birth dates, record numbers) by column header, plus an SSN-content fallback for a mislabeled column; `ingest_file_with_phi_gate()` is the real enforcement point, taking injected `store`/`log_audit` callables (the real Supabase write and logger don't exist yet -- STORY-005 isn't built) so that a clean file is stored and a PHI-carrying file is rejected before `store` is ever called, with `log_audit` always receiving an audit entry naming the column/category but never the row content; `pipeline/test_phi_guardrail.py` (22 tests); `.colaberry/enrichment/STORY-010.json`
  - Verification: `pytest pipeline/` 52/52 passed (30 existing + 22 new, zero regressions); `.colaberry/progress.json` STORY-010 ticked 3/3, each criterion backed by a named test (`test_gate_rejects_a_file_with_patient_names_before_storage`, `test_gate_rejects_a_file_with_birth_dates_before_storage`, `test_gate_audit_log_never_contains_the_raw_row_content` / `test_gate_birth_date_value_never_reaches_the_audit_log`)
  - Notes: found mid-task that a bare detector wouldn't actually earn these criteria -- REQ-009 says "reject... before it is stored or logged," which is gating behavior, not classification, and STORY-011's `grounding_guardrail.py` sat unticked for exactly this reason (nothing calls it yet). Built the gate around injected store/log_audit instead of waiting for STORY-005. Also found and fixed a discrepancy while starting this story: `.colaberry/plan.json` and `.hospulse/plan.json` both define a "STORY-010" but they are different stories (this one vs. "email the weekly briefing," which is `.hospulse`'s STORY-010) -- built under `.colaberry`'s definition per the portal task and user confirmation. Left `docs/stories/STORY-010.md` (and the rest of `docs/stories/*.md` except STORY-000.md) un-rewritten: they still reflect `.hospulse`'s old numbering repo-wide, and fixing just one file would leave a half-migrated, inconsistent set -- flagged to the user as a separate item rather than patched here.

- [x] STORY-003 (`.colaberry` numbering): Public Health Snapshot Access -- free public search + three-year trends
  - Date: 2026-09-23
  - What changed: scaffolded a Next.js 16 + TypeScript app at `frontend/` (App Router, `src/`, ESLint) using `create-next-app`; added `@supabase/supabase-js` with `frontend/.env.local` (gitignored) holding only `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY`, the public anon key, never `service_role`; `frontend/src/lib/supabaseClient.ts`; `frontend/src/lib/hospitalSearch.ts` (case-insensitive partial name match against `hospitals`) and `frontend/src/lib/hospitalDetail.ts` (latest 3 fiscal years from `cost_report_years` by CCN, ascending, never padded to look complete); `frontend/src/app/page.tsx` (search form, not-found and data-error messages) and `frontend/src/app/hospital/[ccn]/page.tsx` (trend table with fiscal year per row, a static CMS filing/processing-lag disclosure, and an explicit "reporting gap, not an error" note when fewer than 3 years exist); `frontend/src/lib/hospitalSearch.test.ts` (5 tests) and `frontend/src/lib/hospitalDetail.test.ts` (7 tests); `.colaberry/progress.json` STORY-003 ticked 3/3; `.colaberry/enrichment/STORY-003.json`
  - Verification: `npx vitest run` 12/12 passed; `npx tsc --noEmit` clean; re-verified live against production Supabase data via a real running dev server (not just mocks) -- name search for "Anson" returns ANSON GENERAL HOSPITAL, a garbage name returns the not-found message, CCN 370149 (ST ANTHONY SHAWNEE HOSPITAL) renders all three real fiscal years 2023/2024/2025 in order, CCN 670781 (ANSON GENERAL HOSPITAL, a real 2-year gap) renders only its genuine 2 years plus the reporting-gap note instead of fabricating a third, and a bogus CCN returns the hospital-not-found page
  - Notes: no Supabase client credentials (project URL + anon key) existed locally at the start -- only `DATABASE_URL`, a direct Postgres string used by the Python pipeline. Considered querying Postgres directly from the frontend to avoid needing a new credential, but rejected it: Vercel serverless functions are ephemeral and would exhaust Supabase's direct-connection limit under real traffic, which is exactly why Supabase fronts Postgres with a pooler/REST layer for serverless clients -- got the anon key from the user instead and built on `supabase-js`. Same `.colaberry` vs `.hospulse` STORY-003 numbering collision already documented for STORY-010: `.hospulse`'s STORY-003 is a different story ("write a grounded hospitals-at-risk briefing," release r0) from `.colaberry`'s STORY-003 built here (release r1, public search). Built under `.colaberry`'s definition per the portal task, matching the STORY-010 precedent, not re-litigated. The "notes public data lag" trust criterion is a static disclosure sentence about CMS's filing-and-processing delay, not a computed per-row lag figure -- there is no lag timestamp in the source data to compute one from, and inventing a number would violate the project's own grounding guardrail (REQ-004). Not yet committed to git as of this entry -- pending explicit go-ahead to commit.

- [x] STORY-004 (`.colaberry` numbering): Implement Early Warning Flags
  - Date: 2026-09-23
  - What changed: `pipeline/schema.sql` -- new `early_warning_flags` table (PK `provider_ccn`, public-read RLS matching `hospitals`/`cost_report_years`), one current-state row per hospital, replaced on each run; `pipeline/early_warning.py` -- pure `evaluate_early_warning_flags()` implementing REQ-004's three criteria (operating margin below 0%, days cash on hand below 30, days in A/R rising two consecutive fiscal years) plus `fetch_all_hospital_years()`/`upsert_flags()`/`run()` for the live Supabase wiring; `pipeline/test_early_warning.py` (10 tests)
  - Verification: `pytest pipeline/` 62/62 passed (52 existing + 10 new, zero regressions); ran for real against all 274 hospitals in production Supabase (257 flagged, 16 not assessable, 1 genuinely healthy -- RICE MEDICAL CENTER, CCN 451312, spot-checked its exact stored values: 5.84% margin, 55.8 days cash, falling A/R, correctly not flagged); ran the evaluator twice against production to confirm idempotency (274 rows before and after, no duplicates, identical distribution); `.colaberry/progress.json` STORY-004 ticked 3/3
  - Notes: missing data is never treated as "no problem" -- a criterion with a null required metric is `not_assessable`, not `not_triggered`, and overall hospital status uses the precedence flagged > not_assessable > not_flagged so a real trigger is never hidden behind an unrelated missing metric. "Days in A/R rising two years in a row" requires three *consecutive* fiscal years, not just the latest three rows on file -- a hospital with a real reporting gap (already known to exist in this dataset, e.g. CCN 670781 missing FY2023) is correctly `not_assessable` for that one criterion rather than comparing non-adjacent years as if they were adjacent. The flag evaluator reuses the exact "latest 3 fiscal years, ascending" query shape from `frontend/src/lib/hospitalDetail.ts` so it can never disagree with the public Health Snapshot page about which years count as "the latest three." The 257/274 flag rate is a real finding about this population (rural/Critical Access hospitals nationally run on thin or negative margins), not a logic bug -- verified by spot-checking individual hospitals' exact stored trigger values against their verdict, not just trusting the aggregate count. Not yet committed to git as of this entry -- pending explicit go-ahead to commit.

- [x] STORY-011 (`.colaberry` numbering): Convert Hospital System Exports to Standard Metrics
  - Date: 2026-09-23
  - What changed: `pipeline/schema.sql` -- two new tables, `export_conversions` (one audit row per file processed, `source_file_hash` UNIQUE for idempotency) and `hospital_monthly_metrics` (one row per hospital/month/metric, UNIQUE on `(provider_ccn, month, metric_name)` for idempotent upsert), both RLS-enabled with zero policies (fails closed -- private management-company data, unlike the public CMS tables, and no per-company scoping exists yet); `pipeline/mappings/` -- a per-vendor mapping registry (`__init__.py`'s `detect_format()` matches by exact column-fingerprint subset, never fuzzy; `epic.py` and `cerner.py`, two illustrative source-system layouts; `_util.py`'s `to_float()` shared CSV-string-to-number coercion); `pipeline/export_normalizer.py` -- `normalize_rows()` (pure: detects format, drops totals rows, maps to standard metric names) and `run()` (DB-wired: idempotency check, PHI gate, persist); wired `pipeline/phi_guardrail.ingest_file_with_phi_gate()` (STORY-010) into `run()` so REQ-009 applies to this ingestion path too; extracted `load_env()` out of `hcris_import.py`/`early_warning.py` into `pipeline/env.py` once a third file needed it; `pipeline/test_export_normalizer.py` (14 tests); `.colaberry/progress.json` STORY-011 ticked 3/3; `.colaberry/enrichment/STORY-011.json`
  - Verification: `pytest pipeline/` 76/76 passed (62 existing + 14 new, zero regressions after the `load_env` refactor); live against production Supabase -- Epic and Cerner fixtures for the same hospital/month produced identical standard metrics (`cash_on_hand`, `ar_balance`, `denial_rate_pct`, `open_positions`); re-running identical file content under a different filename returned the first run's outcome with no second `export_conversions` row (idempotency via SHA-256 content hash); an unmapped format returned `needs_mapping`; an Epic-shaped export with an added "Patient Name" column was rejected (`PhiRejectedError`) with zero rows written to either new table and a structured audit log line containing no PHI content; a clean file re-verified working immediately after. All live test rows deleted from the production tables afterward.
  - Notes: third instance of the `.colaberry`/`.hospulse` numbering collision already documented for STORY-010 and STORY-003 -- `.hospulse`'s own STORY-011 is an unrelated cost-report-co-pilot story (REQ-015, release r4), and `.hospulse`'s equivalent of this REQ-011 requirement is its own STORY-008 (also not started). Built under `.colaberry`'s definition per the same precedent, `docs/stories/STORY-011.md` left un-rewritten for the same reason as before. No operator-upload path exists yet (STORY-005, not started), so "an export from a hospital system" is raw file bytes handed directly to `export_normalizer.run()` -- the upload UI itself is out of scope here. Epic/Cerner column layouts are illustrative, not sourced from a real vendor export sample (none was available) -- unlike `hcris_import.py`'s worksheet citations, which were all confirmed against a real downloaded file; a real vendor mapping would need a real sample to build against, flagged as a known limitation rather than presented as verified against real data. Found mid-task that the PHI guardrail built for STORY-010 wasn't being called from this ingestion path even though REQ-009 is a whole-project guardrail, not scoped to STORY-010 alone -- the same lesson this repo already learned once for STORY-011's own grounding guardrail sitting unticked until something called it (see STORY-010's note). A PHI rejection raises rather than folding into `ConversionOutcome.status`, on purpose: a security rejection and a normal/retryable conversion outcome must not be representable the same way to a caller that only checks `.status`. Not yet committed to git as of this entry -- pending explicit go-ahead to commit.

- [x] STORY-012 (`.colaberry` numbering): Surface Slipping Hospitals Early
  - Date: 2026-09-23
  - What changed: `pipeline/schema.sql` -- new `slipping_hospital_alerts` table (PK `provider_ccn`, one current-state row per hospital, replaced on each run), RLS-enabled with zero policies -- deliberately unlike `early_warning_flags`' public-read policy, since this is derived from `hospital_monthly_metrics` (STORY-011's private, operator-uploaded data), not public CMS data; `pipeline/slipping_detector.py` -- pure `evaluate_slipping()` implementing 3 month-over-month criteria (`cash_on_hand` declining, `denial_rate_pct` rising, `ar_balance` rising between the two latest consecutive months) plus `fetch_all_hospital_months()` (pivots `hospital_monthly_metrics`' long rows into one `MonthMetrics` per hospital/month)/`upsert_alerts()`/`run()` for the live Supabase wiring; `pipeline/test_slipping_detector.py` (12 tests); `.colaberry/progress.json` STORY-012 ticked 3/3; `.colaberry/enrichment/STORY-012.json`
  - Verification: `pytest pipeline/` 88/88 passed (76 existing + 12 new, zero regressions); live end to end through the real STORY-011 pipeline (not synthetic rows) -- normalized two real consecutive months' exports for one hospital (cash $200k→$150k, denial rate 5%→9%, AR balance unchanged), then ran the detector and confirmed it correctly flagged `slipping` with `cash_on_hand_declining`/`denial_rate_rising` both triggered (exact before/after values stored) and `ar_balance_rising` correctly `not_triggered`; re-ran the detector immediately after and confirmed idempotency (same single row, not a duplicate). All live test rows deleted from the production tables afterward.
  - Notes: REQ-016 ("surface a slipping hospital at least 4 weeks earlier than the operator's current reporting cycle") names no timestamped "operator reporting cycle" anywhere in this system to literally measure elapsed time against, and gives no numeric thresholds (unlike REQ-004/REQ-005's explicit "below 0%"/"below 30 days"). Both were raised with the user and confirmed before building rather than inventing an unstated duration or magic number: "4 weeks earlier" is implemented structurally -- detection runs automatically the moment a month's export is normalized (verified live: flagged in the same run, no separate review step), rather than waiting for an operator's own roughly-monthly manual review -- and each criterion triggers on a plain month-over-month directional change rather than an invented percentage threshold. Same missing-data philosophy as STORY-004: a criterion with a null required metric, or whose two latest months are not genuinely consecutive calendar months (a gap, not just "the two most recent rows"), is `not_assessable`, never a guessed verdict; precedence is slipping > not_assessable > not_slipping. Fourth instance of the `.colaberry`/`.hospulse` numbering collision, but with no reusable local blueprint this time -- `.hospulse`'s STORY-012 is an unrelated co-pilot sign-off story, and `.hospulse`'s own REQ-016 equivalent is just a secondary tag on its STORY-010 (a different, unrelated weekly-briefing-email story from this repo's own STORY-010). Built under `.colaberry`'s definition per the same precedent. Not yet committed to git as of this entry -- pending explicit go-ahead to commit.

- [x] STORY-005 (`.colaberry` numbering): Operator Upload Functionality
  - Date: 2026-09-23
  - What changed: `pipeline/export_normalizer.py` -- added `is_duplicate` to `ConversionOutcome` (via a new pure `_outcome_from_existing()` helper) and a `--json` CLI mode emitting one stable, parseable result line; `pipeline/test_export_normalizer.py` (+2 tests); `frontend/src/lib/exportUpload.ts` -- pure `parseNormalizerOutput()`/`httpStatusFor()`/`messageFor()`, the typed bridge contract; `frontend/src/lib/exportUpload.test.ts` (12 tests); `frontend/src/app/api/upload/route.ts` -- the actual bridge: saves the upload to a temp path, invokes `export_normalizer.py --json` via `child_process`, maps its output to a real HTTP status (200/409/422/400/500), always cleans up the temp file; `frontend/src/app/upload/page.tsx` -- minimal operator upload form; `@playwright/test` + Chromium added as a frontend dev dependency, `frontend/playwright.config.ts` (attaches to an already-running dev server via `reuseExistingServer` rather than conflicting with one), `frontend/e2e/upload.spec.ts` (3 real-browser tests, DB rows cleaned up in an `afterAll` hook), `frontend/.gitignore` (+`test-results`/`playwright-report`); `.colaberry/progress.json` STORY-005 ticked 3/3; `.colaberry/enrichment/STORY-005.json`
  - Verification: `pytest pipeline/` 90/90 passed; `npx vitest run` (frontend) 24/24 passed; `npx tsc --noEmit` clean; `npx eslint` clean on all new files; live end to end against the real running dev server via curl -- a fresh valid export returned 200 with 4 metrics normalized; the identical file re-uploaded returned 409 duplicate with zero new `export_conversions` rows (confirmed by direct query); an unrecognized format returned 422 `needs_mapping`; a PHI-bearing export returned 422 with zero rows in either table; an empty request body returned a clean 400 after a fix (see Notes). Then re-verified with `npx playwright test` -- 3/3 passed driving a real headless Chromium browser against the actual `/upload` page (not curl, not a mock): valid export → accepted message, identical re-upload → duplicate message, PHI-bearing export → rejected message. Suite run twice back to back to confirm it's genuinely re-runnable (fresh unique content per run, not a fixed fixture that would collide with itself). All live test rows and temp files cleaned up afterward, confirmed empty by direct query.
  - Notes: the story's own suggested approach (Next.js + Supabase storage, writing via `supabase-js`) could not actually work as suggested -- the frontend only has `NEXT_PUBLIC_SUPABASE_ANON_KEY`, and `export_conversions`/`hospital_monthly_metrics` (STORY-011) have RLS enabled with zero policies on purpose, so the anon key cannot read or write them. Raised with the user, who deferred to best judgment; built instead as a subprocess bridge to the already-built, already-tested `pipeline/export_normalizer.py` rather than reimplementing hash-dedup, format detection, or the PHI gate in TypeScript -- `DATABASE_URL` is read only by the Python subprocess from the repo-root `.env`, never touching the Node process or the browser bundle, so no new secret was needed. Found two real bugs via live testing, not just the happy path: (1) the PHI gate's audit log line prints to stdout *before* the CLI's own `--json` result line, so a naive "stdout is one JSON line" assumption would have broken on the very first PHI scan -- fixed by parsing the last line, not assuming a single line, with a test locking this in; (2) `request.formData()` itself throws on an empty/malformed body, before any of the route's own file-presence validation runs, surfacing live as an unhandled 500 with no body instead of a clean 400 -- fixed by wrapping the parse itself in a try/catch. Chose a JSON API response with real per-outcome HTTP statuses over a redirect-based no-JS form flow (unlike STORY-003's plain search form), since it's directly verifiable with curl and is the more idiomatic pattern for a file-upload interaction. Did not build Supabase Storage for the raw file -- the acceptance criteria test accept/process/duplicate-rejection/hash-dedup, not file retrievability, and a storage bucket would add integration surface (policies, another credential) nothing here actually requires yet. The user asked how to get real browser automation after the initial curl-only verification; installed `@playwright/test` and Chromium as a frontend dev dependency (matching this project's own stated standard for browser E2E work) rather than needing a new MCP tool, and re-verified the `/upload` page with a real headless-Chromium-driven test suite -- this closes the earlier "no browser automation available" gap rather than leaving it as a standing limitation. Found live while wiring the Playwright config that another `next dev` server for this same repo was already running on port 3000 from an earlier session -- used `reuseExistingServer` to attach to it instead of conflicting, per this project's own concurrent-instance-safety rule. Also raised separately with the user: the current 13-story `.colaberry` plan (r0 through r4) has no dedicated UI/visual-design story anywhere in it -- every page built so far, including this one, is intentionally plain and functional ("walking skeleton first"), and a real design pass is a scope decision still pending, not something assumed into this story. Not yet committed to git as of this entry -- pending explicit go-ahead to commit.

## Next

- [ ] Coffee with prospect #1 (4 validation checks: pain, data access, budget owner, existing tools)
- [ ] Health Snapshot v0 from public CMS HCRIS cost report data (OK + TX rural hospitals)
- [ ] LinkedIn Post 1 (RHTP / visibility problem)
- [ ] Build STORY-011 / STORY-003 (`.hospulse` numbering) for real and wire them to call `pipeline/grounding_guardrail.validate_ai_output_is_grounded`
- [ ] Deploy the frontend to Vercel (production traffic path) once more of `.colaberry`'s r1 stories are built -- raised and deliberately deferred during STORY-003, per user
